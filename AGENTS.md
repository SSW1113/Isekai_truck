# 이세계 트럭 키우기 - Unity 개발 지침

## 프로젝트 개요

현재 프로젝트는 Unity로 이전이 완료된 3D 웹게임 **이세계 트럭 키우기**다.

플레이어는 트럭을 조작하여 맵을 돌아다니며 몬스터를 치어 처치한다.
몬스터를 처치하면 EXP와 영혼을 획득하고 레벨업 및 트럭 업그레이드가 가능하다.

앞으로의 개발은 **현재 Unity 버전을 기준 구현(reference implementation)** 으로 한다.
기존 Three.js 버전은 과거 동작을 비교할 필요가 있을 때만 참고한다.

## 기본 원칙

- 현재 정상 동작하는 기능을 임의로 삭제하거나 크게 변경하지 않는다.
- 기능을 추가할 때 현재 플레이 감각을 최대한 유지한다.
- 한 클래스에 여러 시스템의 책임을 몰아넣지 않는다.
- 사용자가 요청하지 않은 대규모 리팩터링을 하지 않는다.
- 수정 전에 관련 파일과 호출 관계를 먼저 확인한다.
- 기존 코드의 네이밍, 들여쓰기, 줄바꿈, 주석 방식 등 현재 코드 컨벤션을 유지한다.
- 새 기능은 기존 시스템과 강하게 결합되지 않도록 설계한다.
- 향후 기능 추가를 고려하되, 아직 필요하지 않은 시스템을 미리 과도하게 구현하지 않는다.
- 확장성을 이유로 불필요한 추상화, 인터페이스, 상속 구조를 남발하지 않는다.
- 기존 구현과 AGENTS.md의 설명이 다르면 임의로 추측하지 말고 실제 코드를 우선 확인한다.

## Unity C# 코드 컨벤션

### 네이밍

- 클래스, 구조체, enum, public 메서드, 프로퍼티는 PascalCase를 사용한다.
- private 필드와 지역 변수는 camelCase를 사용한다.
- bool 변수는 가능하면 `is`, `has`, `can`, `should` 등 의미가 명확한 형태를 사용한다.
- 상수는 프로젝트의 기존 스타일을 우선하며, 새로 만드는 경우 의미가 분명한 이름을 사용한다.
- 축약어보다 역할이 바로 이해되는 이름을 우선한다.

예:

```csharp
private float currentSpeed;
private bool isFleeing;

public void UpgradeSpeed()
{
    // ...
}
```

### 들여쓰기와 줄바꿈

- 들여쓰기는 공백 4칸을 사용한다.
- 중괄호는 일반적인 C# Allman 스타일을 사용한다.
- 짧은 변수 선언과 계산식은 불필요하게 여러 줄로 나누지 않는다.
- 긴 조건식이나 계산식은 가독성이 실제로 좋아지는 경우에만 줄바꿈한다.
- 사용자가 요청하지 않은 전체 파일 포맷팅은 하지 않는다.

### 주석

- 주석은 짧고 기능 중심으로 작성한다.
- 코드만 읽어도 알 수 있는 내용을 장황하게 설명하지 않는다.
- 복잡한 알고리즘, 중요한 설계 의도, 기존 동작을 유지해야 하는 이유가 있을 때만 설명을 추가한다.

예:

```csharp
// 트럭 이동
// 몬스터 AI
// 몬스터 충돌
// 배회 방향 변경
```

### 필드와 Inspector

- Inspector 노출이 필요하지 않은 필드를 `public`으로 만들지 않는다.
- Inspector에서 설정해야 하는 값은 가능하면 `[SerializeField] private`를 사용한다.
- 런타임 상태값과 게임 설정값을 명확히 구분한다.
- 다른 클래스가 값을 직접 수정하게 하기보다 필요한 메서드나 읽기 전용 프로퍼티를 제공한다.

## 클래스 책임 분리

각 클래스는 가능한 한 하나의 명확한 책임을 가진다.

현재 구조와 유사한 역할 분리를 유지한다.

```text
GameManager
→ 게임 시스템 연결
→ 전체 게임 상태 흐름

GameConfig / ScriptableObject
→ 공통 밸런스 및 설정값

WorldManager
→ 월드 타일/청크
→ Fog
→ 플레이 범위 관리

JoystickInput
→ 플레이어 입력

TruckController
→ 트럭 이동
→ 트럭 자체 상태
→ 속도/크기 업그레이드 적용

CameraController
→ 트럭 추적
→ 트럭 실제 크기에 따른 줌 조절

MonsterController
→ 개별 몬스터 상태와 AI

MonsterManager
→ 몬스터 생성/제거 및 전체 관리가 필요한 경우 담당

MonsterSpawner
→ 언제, 어디에, 어떤 몬스터를 생성할지 결정

PlayerState
→ 레벨
→ EXP
→ 영혼
→ 업그레이드 포인트

GameUIController
→ HUD
→ 업그레이드 UI
→ 게임 UI 표시
```

예를 들어:

- `MonsterSpawner`에 몬스터 AI를 넣지 않는다.
- `TruckController`에 EXP 계산을 넣지 않는다.
- `GameManager`에 모든 시스템 로직을 넣지 않는다.
- UI 스크립트가 게임 밸런스 값을 직접 변경하지 않는다.

## 확장성 설계 원칙

앞으로 몬스터, 스킬, 트럭 업그레이드, 이펙트가 늘어나는 것을 고려한다.

### 1. 데이터와 동작을 분리한다

캐릭터나 몬스터마다 달라지는 수치는 가능한 한 데이터로 관리한다.

특정 몬스터마다 아래처럼 ID를 직접 비교하는 코드는 가능한 한 피한다.

```csharp
if (monsterId == "policeman")
{
    // 경찰 전용 로직
}
```

몬스터 종류가 늘어날수록 조건문이 계속 증가하는 구조를 만들지 않는다.
특수 행동이 필요해질 때는 behavior/state/strategy 형태로 분리할 수 있도록 설계한다.

단, 몬스터 종류가 적고 로직도 단순한 단계에서는 불필요하게 복잡한 패턴을 먼저 만들지 않는다.

### 2. 설정값을 로직에 하드코딩하지 않는다

밸런스 값은 가능한 한 설정 클래스 또는 ScriptableObject에서 관리한다.

좋은 예:

```csharp
float directionLockDistance = collisionDistance * monsterConfig.directionLockMultiplier;
```

피해야 할 예:

```csharp
float directionLockDistance = collisionDistance * 1.72f;
```

단, 현재 프로젝트가 이미 특정 설정 관리 방식을 사용하고 있다면 그 방식을 우선한다.

### 3. 시스템 간 직접 참조를 최소화한다

한 시스템이 다른 시스템 내부 상태를 직접 수정하는 방식을 피한다.

예를 들어 몬스터 처치 시:

```text
Monster
→ 처치 발생
→ 보상 이벤트/메서드 호출
→ PlayerState가 EXP/영혼 처리
→ UI가 변경된 상태를 표시
```

처럼 역할을 분리한다.

`MonsterController`가 직접 UI Text를 수정하거나,
`GameUIController`가 몬스터를 제거하는 구조를 만들지 않는다.

### 4. 새 기능은 기존 시스템에 끼워 넣을 수 있어야 한다

향후 예정된 기능:

- 트럭 스킬
- 일시적 거대화
- 특수 몬스터
- 몬스터별 특수 AI
- 몬스터 등급
- 레벨 기반 스폰 밸런싱
- 파티클/스킬 이펙트
- 실제 3D 모델과 애니메이션

이 기능들을 추가하기 위해 기존 핵심 클래스를 매번 대규모 수정해야 하는 구조를 피한다.

예를 들어 스킬 시스템을 만들 경우:

```text
TruckController
→ 트럭의 최종 상태 적용

SkillSystem
→ 어떤 스킬이 활성화되어 있는지 관리

Size Skill
→ 크기 배율 제공
```

처럼 트럭 이동 코드와 스킬 로직이 서로 뒤섞이지 않도록 한다.

### 5. 상태와 최종 값을 구분한다

향후 버프/디버프/일시적 효과를 고려해,
기본 능력치와 현재 최종 능력치를 구분할 수 있도록 설계한다.

예:

```text
기본 크기
× 업그레이드 배율
× 일시적 스킬 배율
= 현재 실제 크기
```

스킬이 `Transform.localScale` 값을 임의로 저장해 덮어쓰고,
종료 시 이전 값을 추측해서 복구하는 방식은 피한다.

속도, 크기 등 여러 시스템이 수정할 가능성이 있는 수치도 같은 원칙을 적용한다.

### 6. Unity 컴포넌트 의존성을 명확히 한다

필수 컴포넌트가 있다면 코드에서 명확하게 표현한다.
필요한 경우 `[RequireComponent]`를 사용할 수 있다.

단, 모든 객체를 MonoBehaviour로 만드는 방식은 피한다.
Scene 오브젝트 생명주기가 필요한 클래스만 MonoBehaviour를 사용한다.

### 7. 이벤트는 필요한 곳에만 사용한다

시스템 간 결합도를 낮출 필요가 있을 때 C# event/Action 등을 사용할 수 있다.

하지만 단순한 직접 메서드 호출이 더 명확한 경우에는 확장성이라는 이유만으로 이벤트 버스를 만들지 않는다.

목표는 **낮은 결합도와 높은 가독성의 균형**이다.

## 성능 및 런타임 원칙

이 게임은 많은 몬스터가 동시에 존재할 수 있으므로 런타임 할당에 주의한다.

- `Update()`에서 불필요한 `new` 사용을 피한다.
- 매 프레임 새로운 List나 배열을 만들지 않는다.
- `GameObject.Find`, `FindObjectOfType` 같은 전역 검색을 반복 호출하지 않는다.
- 자주 사용하는 컴포넌트 참조는 캐싱한다.
- 반복적으로 생성/삭제되는 몬스터와 이펙트는 필요할 경우 Object Pool을 사용한다.
- 몬스터 수가 많아질 경우 모든 몬스터가 무거운 로직을 매 프레임 수행하지 않도록 최적화를 고려한다.
- 최적화 전에는 실제 병목을 확인하고, 플레이 감각을 해치는 성급한 최적화는 하지 않는다.

Web 빌드도 목표이므로:

- 불필요하게 큰 텍스처/모델 사용을 피한다.
- 사용하지 않는 에셋과 패키지를 프로젝트에 남기지 않는다.
- 런타임 메모리 사용을 고려한다.

## 기존 플레이 동작 보존

기능 추가나 리팩터링 과정에서 요청받지 않은 기존 플레이 동작을 변경하지 않는다.

### 트럭

- 조이스틱 입력 방향은 목표 회전 방향을 결정한다.
- 트럭은 현재 바라보는 방향으로 전진한다.
- 입력 중 가속한다.
- 입력 해제 시 마지막 진행 방향으로 짧은 관성이 있다.
- 현실적인 WheelCollider 차량 물리로 임의 변경하지 않는다.

현재 기준값은 실제 프로젝트 설정을 우선 확인한다.

### 몬스터

평상시:
- 랜덤 방향 배회
- 배회 속도는 기본 도망 속도보다 느리다.

트럭 감지:
- `fleeDistance` 안에서 트럭 반대 방향으로 도망간다.
- 도망 속도를 임의로 낮추지 않는다.

가까운 거리:
- `directionLockDistance` 안에서는 마지막 도망 방향을 유지한다.
- 현재 기준 `directionLockMultiplier = 1.72`

다음은 이전에 테스트 후 롤백된 동작이므로 다시 넣지 않는다.

- 느린 방향전환
- 도망 방향 업데이트 딜레이
- 근거리 도망 속도 감소
- 커브 감속
- flee acceleration

### 트럭 크기

충돌 판정, 몬스터 방향 고정 거리, 카메라 줌 등
크기에 영향을 받는 시스템은 **현재 실제 트럭 크기**를 기준으로 판단한다.

향후 일시적 거대화 스킬도 자동 반영될 수 있는 구조를 유지한다.

### 카메라

- 트럭 위치를 따라간다.
- 트럭이 회전해도 카메라가 같이 회전하지 않는다.
- 실제 트럭 크기가 증가하면 줌아웃한다.
- 줌 증가에 따라 필요한 월드 표시 범위도 확장한다.

### 스폰

현재 `spawnWeight`는 플레이어 레벨에 따라 자동 변경되지 않는다.

과거 논의 후 롤백된:
- 레벨 기반 몬스터 수 증가
- 레벨 기반 스폰 주기 변경
- 몬스터 등급에 따른 spawnWeight 변경

기능은 사용자가 다시 요청하기 전까지 구현하지 않는다.

## 리팩터링 원칙

리팩터링은 다음 조건에서만 수행한다.

- 중복 코드가 실제 유지보수 문제를 만들고 있을 때
- 새 기능 추가가 현재 구조 때문에 과도하게 어려울 때
- 클래스가 여러 책임을 동시에 담당하게 되었을 때
- 사용자 요청이 있을 때

리팩터링 시:

1. 기존 동작을 먼저 파악한다.
2. 동작과 구조 변경을 동시에 하지 않는다.
3. 가능하면 작은 단위로 나눈다.
4. 컴파일 오류를 확인한다.
5. 기존 게임 동작이 유지되는지 확인한다.
6. 관계없는 파일까지 함께 정리하지 않는다.

## 새 기능 구현 절차

Codex는 새 기능을 구현할 때 다음 순서를 따른다.

1. 관련 스크립트와 데이터 구조를 먼저 읽는다.
2. 현재 시스템의 책임과 의존 관계를 확인한다.
3. 기존 구조에서 확장 가능한 지점을 찾는다.
4. 필요한 경우 최소한의 새 클래스/데이터 구조를 추가한다.
5. 기존 클래스 수정 범위를 최소화한다.
6. 컴파일 오류를 확인한다.
7. 기존 플레이 동작에 영향을 주는지 확인한다.
8. 변경 내용을 사용자에게 요약한다.

작업 후 다음 항목을 간단히 보고한다.

```text
변경한 파일
추가한 기능
기존 시스템과 연결한 방식
확장성을 위해 분리한 부분
기존 동작 중 유지한 부분
추가로 확인할 사항
```

## 금지 사항

사용자의 명시적 요청 없이 다음 작업을 하지 않는다.

- 프로젝트 전체 구조 재설계
- 대규모 파일/클래스명 변경
- ECS/DOTS 전환
- WheelCollider 기반 차량 시스템 전환
- 새로운 외부 프레임워크 도입
- 새 Unity 패키지 무단 설치
- 게임 밸런스 값 임의 변경
- 카메라 조작 방식 변경
- 롤백된 기능 재도입
- 미래 기능을 추측하여 대규모 선행 구현
- 사용하지 않는 추상 클래스/인터페이스 대량 생성
- 모든 시스템을 Singleton 또는 GameManager에 집중
